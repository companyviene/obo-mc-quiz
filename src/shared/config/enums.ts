export enum AppRoute {
  Home = '/',
  Module = '/modules/[id]',
  Player = '/player/[questionId]',
}

export enum StorageKey {
  VideoCache = 'video_cache_map',
}

export enum QueryKey {
  Catalog = 'catalog',
  Module = 'module',
}
